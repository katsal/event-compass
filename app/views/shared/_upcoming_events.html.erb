<% @upcoming_events.each do |upcoming_event| %>
  <div class="py-3">
    <div class="card-trip m-auto " style="width: 18rem">
      <h2 class="card-trip-pricing">
        <% if upcoming_event.price =~ /free/i || upcoming_event.price !~ /\d/ %>
          Free
        <% elsif upcoming_event.price.length > 6 %>
          <%= truncate(upcoming_event.price, length: 10, omission: '...') %>
        <% else %>
          <%= upcoming_event.price %>
        <% end %>
      </h2>
      <div class="card-trip-infos">
        <div class="card-trip-header">
          <h2><%= truncate(upcoming_event.name, length: 15, omission: '...') %></h2>
        </div>
        <div class="pt-3">
          <div class="event-dates d-flex flex-row gap-2">
            <div>
              <i class="primary fa-regular fa-calendar"></i>
            </div>
            <div>
              <span><%= upcoming_event.start_date.strftime('%d %B') %></span>
              <% if upcoming_event.end_date && upcoming_event.end_date.to_date !=  upcoming_event.start_date.to_date %>
                <span> - <%= upcoming_event.end_date.strftime('%d %B') %></span>
              <% end %>
            </div>
          </div>
        </div>
      </div>
      <%= link_to "", event_path(upcoming_event), class: 'card-link' %>
    </div>
  </div>
<% end %>
